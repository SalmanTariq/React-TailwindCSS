import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const UserSlice = createSlice({
  name: 'users',
  initialState,
  reducers: {
    getUser: (state, action) => {},
  },
});
